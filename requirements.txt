# BDDS Performance Statistics Workflow Dependencies
#
# flask_restx and requests are already provided by the Gateway runtime (used by the
# isp-forwarding and multiple-view-operations workflows), listed here for documentation only.
flask_restx==1.3.0
requests>=2.28.0
